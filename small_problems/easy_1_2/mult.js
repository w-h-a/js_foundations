// 1. Takes two numbers, multiplies them together, and returns the result.

function mult(firstNumberParam, secondNumberParam) {
  return firstNumberParam * secondNumberParam;
}

console.log(mult(3, 5));
