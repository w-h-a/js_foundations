let foo = 1;
function bar() {
  let xyz = 3;
  const qux = 5;
  return qux;
}

const yam = function() {};
bar();

/*
As we write more code below,
which names used above are
in scope for our continued use?
*/
